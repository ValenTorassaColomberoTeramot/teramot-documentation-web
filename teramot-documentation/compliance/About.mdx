import React, { useState } from 'react';
import useBaseUrl from '@docusaurus/useBaseUrl';

export default function AboutPage() {
  const [lang, setLang] = useState('en');

  const renderContent = () => {
    if (lang === 'en') {
      return (
        <>
          <h1>About this Compliance Hub</h1>
          <p>
            This section contains all the documentation, policies, evidence and configurations required by our vendors
            to ensure successful ISO 27001 and SOC 2 audits.
          </p>
          <p>
            Teramot understands the importance of the enterprise data we handle — which is why cybersecurity is
            integrated into every level of our operations.
          </p>

          <img src={useBaseUrl('/img/compliance/vanta-logo.jpg')} alt="Vanta logo" width="160" style={{marginRight: '1rem'}} />
          <img src={useBaseUrl('/img/compliance/hackmetrix.jpg')} alt="Hackmetrix logo" width="160" />


          <h2>Security Stack</h2>
          <p>
            We leverage industry-leading tools and cloud security services to ensure protection across infrastructure,
            code, endpoints, and credentials.
          </p>

          <div style={{display: 'flex', flexWrap: 'wrap', gap: '1rem', marginBottom: '1rem'}}>
            <img src={useBaseUrl('/img/compliance/guardduty.jpg')} alt="AWS Security Services" height="70" />
            <img src={useBaseUrl('/img/compliance/bitdefender.jpg')} alt="Bitdefender" height="70" />
            <img src={useBaseUrl('/img/compliance/Bitwarden.png')} alt="Bitwarden" height="70" />
          </div>

          <p>
            We are preparing for our first SOC 2 Type I audit. All controls, monitoring, and policies are being
            continuously reviewed and hardened in partnership with our vendors.
          </p>
        </>
      );
    } else {
      return (
        <>
          <h1>Sobre este Hub de Cumplimiento</h1>
          <p>
            Esta sección contiene toda la documentación, políticas, evidencias y configuraciones que nuestros vendors
            requieren para lograr auditorías exitosas de ISO 27001 y SOC 2.
          </p>
          <p>
            En Teramot comprendemos la importancia de los datos empresariales que gestionamos — por eso la
            ciberseguridad está integrada en cada nivel de nuestras operaciones.
          </p>

          <img src={useBaseUrl('/img/compliance/vanta-logo.jpg')} alt="Vanta logo" width="160" style={{marginRight: '1rem'}} />
          <img src={useBaseUrl('/img/compliance/hackmetrix.jpg')} alt="Hackmetrix logo" width="160" />

          <h2>Pila de Seguridad</h2>
          <p>
            Utilizamos herramientas líderes en la industria y servicios de seguridad en la nube para proteger nuestra
            infraestructura, código, endpoints y credenciales.
          </p>

          <div style={{display: 'flex', flexWrap: 'wrap', gap: '1rem', marginBottom: '1rem'}}>
            <img src={useBaseUrl('/img/compliance/guardduty.jpg')} alt="AWS Security Services" height="70" />
            <img src={useBaseUrl('/img/compliance/bitdefender.jpg')} alt="Bitdefender" height="70" />
            <img src={useBaseUrl('/img/compliance/Bitwarden.png')} alt="Bitwarden" height="70" />
          </div>

          <p>
            Estamos preparando nuestra primera auditoría SOC 2 Tipo I. Todos los controles, monitoreos y políticas se
            revisan y fortalecen continuamente junto a nuestros vendors.
          </p>
        </>
      );
    }
  };

  return (
    <div>
      <div style={{display: 'flex', justifyContent: 'flex-end', marginBottom: '1rem'}}>
        <button onClick={() => setLang('en')} disabled={lang === 'en'}>🇺🇸 English</button>
        <button onClick={() => setLang('es')} disabled={lang === 'es'} style={{marginLeft: '0.5rem'}}>🇪🇸 Español</button>
      </div>
      {renderContent()}
    </div>
  );
}
